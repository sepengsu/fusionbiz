# FastAPI core dependencies
fastapi==0.110.0  # Web framework for building APIs
uvicorn==0.29.0  # ASGI server for running FastAPI
jinja2==3.1.3  # Template engine for rendering HTML
python-multipart==0.0.9  # Support for file uploads in FastAPI

# AI model dependencies
openai==1.12.0  # GPT-4 API for text summarization
whisper==1.1.10  # OpenAI Whisper for speech-to-text (STT)
torch==2.2.2  # PyTorch for running Whisper models

# OCR (Handwritten text extraction)
pytesseract==0.3.10  # OCR engine for extracting text from images
pillow==10.2.0  # Image processing library for handling input images

# File and logging utilities
python-dotenv==1.0.1  # Environment variable management
loguru==0.7.2  # Advanced logging for FastAPI applications

# CORS support (for frontend integration)
fastapi[all]  # Includes WebSockets, CORS, and other optional dependencies
